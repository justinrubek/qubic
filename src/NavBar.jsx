import React from "react";

export default class NavBar extends React.Component {
  constructor(props) {
    super(props);
  }

  render() {
    let items = ["Home", "Gallery"]


    return (
      <nav>
        <p> Hello, World! </p>
      </nav>
    );
  }
}
